# app-environment

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build app-environment` to build the library.

## Running unit tests

Run `nx test app-environment` to execute the unit tests via [Jest](https://jestjs.io).
